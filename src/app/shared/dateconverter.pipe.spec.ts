import { DateconverterPipe } from './dateconverter.pipe';

describe('DateconverterPipe', () => {
  it('create an instance', () => {
    const pipe = new DateconverterPipe();
    expect(pipe).toBeTruthy();
  });
});
